/** @format */

import React from "react";

const Loading = () => {
  return (
    <div
      style={{
        height: "100vh",
        width: "100%",
        position: "absolute",
        top: "0",
        backgroundColor: "#123456",
        color: "white",
        display: "flex",
        justifyContent: "center",
        alignItems: "center",
      }}>
      Loading...
    </div>
  );
};

export default Loading;
