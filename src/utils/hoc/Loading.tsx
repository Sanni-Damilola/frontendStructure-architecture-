/** @format */

import React from "react";

const Loading = () => {
  return (
    <div
      style={{
        width: "100%",
        height: "100vh",
        display: "flex",
        fontSize: "10em",
        alignItems: "center",
        justifyContent: "center",
        backgroundColor: "red",
      }}>
      {" "}
      Loading...
    </div>
  );
};

export default Loading;
